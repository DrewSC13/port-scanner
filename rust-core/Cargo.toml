[package]
name = "rust-core"
version = "0.1.0"
edition = "2021"
description = "Motor Rust para CicadaPort"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "=1.53.1", features = ["rt-multi-thread", "net", "time", "sync"] }
